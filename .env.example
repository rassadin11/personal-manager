# Тайм-зона контейнера (важно для напоминаний и кронов)
TZ=Europe/Moscow

# Все секреты (TELEGRAM_BOT_TOKEN, OPENROUTER_API_KEY и т.д.)
# лежат в ./openclaw.json — этот файл монтируется в контейнер read-only.
# Локальный путь шаблона: скопируй свой ~/.openclaw/openclaw.json в корень
# проекта на VPS под именем openclaw.json (он в .gitignore).
